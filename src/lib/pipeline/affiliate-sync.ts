// Affiliate sync stub — Convex backend not yet connected.
// TODO: replace with Convex mutation once the pipeline is migrated.

export async function syncAffiliateLinks(): Promise<{ synced: number; total: number }> {
  return { synced: 0, total: 0 };
}
