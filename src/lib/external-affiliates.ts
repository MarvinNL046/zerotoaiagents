// External (non-agent) affiliate/partner links used in Watch Me Build posts.
// This file was referenced by the Watch Me Build #02 draft but never committed,
// which broke the Vercel build. Plain vendor URLs for now — replace with real
// affiliate/tracking links once the programs are set up.
export const externalAffiliates = {
  brightData: {
    name: "Bright Data",
    url: "https://brightdata.com",
  },
} as const;
